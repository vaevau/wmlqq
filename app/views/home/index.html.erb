<div class="container">
	<%= render 'sidebar' %>
	<%= render partial: 'chats', locals: {friends: @friends} %>
	<%= render partial: 'chat', locals: {friends: @friends, message: @message} %>
</div>

<%= render 'tools' %>